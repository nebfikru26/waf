# --- DYNAMIC TENANT ROUTES ---
# Generated automatically. Do not edit manually.

# Server block for marelitsolutions.com
server {
    listen 80;
    server_name marelitsolutions.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location / {
        proxy_pass http://213.55.96.5;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for mu.edu.et
server {
    listen 80;
    server_name mu.edu.et;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location / {
        proxy_pass http://213.55.96.6;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for affinisecurity.ai
server {
    listen 80;
    server_name affinisecurity.ai;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=affinisecurity.ai&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://213.55.96.7;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for helu.com
server {
    listen 80;
    server_name helu.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location / {
        proxy_pass http://213.55.96.9;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for aait.edu.et
server {
    listen 80;
    server_name aait.edu.et;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=aait.edu.et&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://10.5.10.3;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger.com
server {
    listen 80;
    server_name trigger.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger2.com
server {
    listen 80;
    server_name trigger2.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger2.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.2;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger3.com
server {
    listen 80;
    server_name trigger3.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger3.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.3;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger4.com
server {
    listen 80;
    server_name trigger4.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger4.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.4;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger5.com
server {
    listen 80;
    server_name trigger5.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger5.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.5;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger6.com
server {
    listen 80;
    server_name trigger6.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger6.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.6;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger7.com
server {
    listen 80;
    server_name trigger7.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger7.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.7;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger8.com
server {
    listen 80;
    server_name trigger8.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger8.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.8;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for trigger9.com
server {
    listen 80;
    server_name trigger9.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=trigger9.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://1.1.1.9;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for localhost
server {
    listen 80;
    server_name localhost;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location / {
        proxy_pass http://host.docker.internal:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for ictcomputer.com
server {
    listen 80;
    server_name ictcomputer.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location /api/waf/verify {
        proxy_pass http://api-dotnet:8080;
    }

    location / {
        if ($http_cookie !~* "affini_clearance=") {
            rewrite ^ /api/waf/challenge?domain=ictcomputer.com&target=$request_uri break;
            proxy_pass http://api-dotnet:8080;
        }
        proxy_pass http://213.55.96.12;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Server block for admintest.com
server {
    listen 80;
    server_name admintest.com;

    # WAF Enabled
    coraza on;
    coraza_rules_file /opt/coraza/config/coraza-rules.conf;

    location / {
        proxy_pass http://host.docker.internal:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

